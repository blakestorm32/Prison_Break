services:
  prison-server:
    build:
      context: ../..
      dockerfile: build/docker/Dockerfile.server
    image: prison-break/server:local
    ports:
      - "8080:8080"
    environment:
      PRISON_SERVER_ADDR: ":8080"
